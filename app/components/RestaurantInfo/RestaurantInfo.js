import React, { Component } from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  View,
  FlatList,
  TouchableOpacity
} from 'react-native';

const restaurantInfo = (props) => (
    <View>
        <Text>hey there</Text>
    </View>
);

export default restaurantInfo;